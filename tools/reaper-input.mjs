import{openPage}from'./cdp.mjs';import{readFileSync}from'node:fs';import assert from'node:assert/strict';const pg=await openPage({port:5996});try{const r=await pg.evalp(readFileSync(new URL('./fixtures/reaper-input-browser.js',import.meta.url),'utf8'));assert.equal(r.rows.length,3);assert.deepEqual(pg.errors,[]);console.log('Full-blood tap casts equipped F skill, hold spends only Surge, and co-op inputs remain independent.');}finally{pg.close();}
