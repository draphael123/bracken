import{openPage}from'./cdp.mjs';import{readFileSync,writeFileSync}from'node:fs';import assert from'node:assert/strict';const pg=await openPage({port:5996});try{const r=await pg.evalp(readFileSync(new URL('./fixtures/economy-browser.js',import.meta.url),'utf8'));for(const row of r.rows)for(const [h,v]of Object.entries(row.heroes))assert(v.cost<=row.available,row.id+'/'+h+' cannot afford available slots');assert.deepEqual(pg.errors,[]);if(process.argv[2])writeFileSync(process.argv[2],JSON.stringify(r,null,2));console.log(r.rows.length+' campaign stages fund each hero\'s available active loadout at half coin collection, after consumable reserve.');}finally{pg.close();}
