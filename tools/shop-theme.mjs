import assert from 'node:assert/strict';import{readFileSync}from'node:fs';import{LEVELS}from'../src/level.js';
const shops=LEVELS.map(l=>l.build()).filter(l=>l.shop);assert.equal(shops.length,3);assert(shops.every(l=>l.music==='store'));const w=readFileSync(new URL('../audio/store.wav',import.meta.url));assert.equal(w.toString('ascii',0,4),'RIFF');assert.equal(w.toString('ascii',8,12),'WAVE');assert.equal(w.readUInt16LE(22),1);assert.equal(w.readUInt32LE(24),22050);assert.equal(w.readUInt16LE(34),16);assert.equal(w.readUInt32LE(40)/(22050*2),40);console.log('All three shops use a valid 40-second original PCM store theme.');
